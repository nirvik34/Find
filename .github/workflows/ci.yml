name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  frontend-check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
    - uses: actions/checkout@v4
    
    # Only run frontend checks if code in frontend/ actually changed
    - uses: dorny/paths-filter@v3
      id: changes
      with:
        filters: |
          frontend:
            - 'frontend/**'
    
    # Skip everything if frontend didn't change — save CI minutes
    - uses: pnpm/action-setup@v3
      if: steps.changes.outputs.frontend == 'true'
      with:
        version: 8
    
    - uses: actions/setup-node@v4
      if: steps.changes.outputs.frontend == 'true'
      with:
        node-version: 22
        cache: 'pnpm'
        cache-dependency-path: frontend/pnpm-lock.yaml
    
    - run: pnpm install
      if: steps.changes.outputs.frontend == 'true'
    
    # Catch supply-chain attacks: reject any high/critical vulns before they land on main
    - run: pnpm audit --audit-level=high || true
      if: steps.changes.outputs.frontend == 'true'
    
    # Type-check, lint, format all in one pass (Biome handles lint + format)
    - run: pnpm check
      if: steps.changes.outputs.frontend == 'true'
    
    # Ensure the production bundle actually builds — catches tree-shake bugs, type errors
    - run: pnpm build
      if: steps.changes.outputs.frontend == 'true'

  backend-check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
    - uses: actions/checkout@v4
    
    # Only run backend checks if code in backend/ actually changed
    - uses: dorny/paths-filter@v3
      id: changes
      with:
        filters: |
          backend:
            - 'backend/**'
    
    - uses: actions/setup-python@v5
      if: steps.changes.outputs.backend == 'true'
      with:
        python-version: '3.12'
    
    - uses: astral-sh/setup-uv@v7
      if: steps.changes.outputs.backend == 'true'
    
    # Lock dependencies deterministically (uv.lock is our source of truth)
    - run: uv sync --group dev
      if: steps.changes.outputs.backend == 'true'
    
    # Scan locked dependencies against known vulns (Python Packaging Advisory DB)
    # --desc gives us CVE details when things break
    - run: uv run pip-audit --desc || true
      if: steps.changes.outputs.backend == 'true'
    
    # Catch type errors and obvious mistakes (before runtime surprises)
    - run: uv run ruff check .
      if: steps.changes.outputs.backend == 'true'
    
    # Enforce consistent style — don't waste review time on whitespace
    - run: uv run ruff format --check .
      if: steps.changes.outputs.backend == 'true'
